# Production builds call the API same-origin (Render rewrites /api/* to the backend),
# so the backend URL prefix is empty. Local dev uses .env instead (http://localhost:8000).
REACT_APP_BACKEND_URL=
