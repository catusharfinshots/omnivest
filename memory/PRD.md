# Basketly — Product Requirements (PRD)

## Original problem statement
"Revamp the full frontend based on the attached design document (basketly-home.html, purple brand)."
Evolved into: a smallcase-style investing platform ("Basketly") with a unified purple brand across all pages,
functional AIF/Advisory lead forms, real JWT auth, and a role-based Admin/Analyst console where analysts
publish model portfolios that admins approve to go live.

## Routes
- `/admin` → **AdminPage** (admins only; analysts are redirected to `/analyst`, investors see a no-access card).
- `/analyst` → **AnalystPage** wrapper → AnalystConsole (analysts only; admins redirected to `/admin`, investors see a "go to dashboard" card). Added Jun 2026 to split the two consoles onto separate URLs.
- Login/signup redirect by role: admin→`/admin`, analyst→`/analyst`, investor→`/dashboard`.

### Phone + OTP auth (Jun 2026)
- Public site login/signup is now **mobile number + SMS OTP via Twilio Verify** for investors AND analysts. Backend `phone_auth.py`: `POST /api/auth/phone/request-otp`, `POST /api/auth/phone/verify-otp` (validates E.164 via `phonenumbers`, 25s resend cooldown, run_in_threadpool for the sync Twilio SDK). New number→investor; valid `invite_code`→analyst (reuses `invites.consume_invite`). Twilio creds in backend/.env; Verify Service `Basketly Login` created via API.
- Users: `email` now optional (phone users have none); unique indexes made **partial** on `email`/`phone` (string only). `create_token`/`public_user` tolerate missing email and expose `phone`.
- Admin remains email/password (internal, `/login`), unchanged.
- Frontend: global `PhoneAuthModal` (2-step phone→OTP) opened via `openAuth()` from AuthContext. Nav shows only **"Get started"** (Login + standalone Connect-broker pill removed; Connect-broker moved into the logged-in account menu). Home hero CTA, `/signup` (incl. `?invite=`), dashboard/invest/analyst auth-gates all open the modal.
- IMPORTANT: 4xx (not 5xx) used for expected OTP errors so the ingress doesn't replace the JSON with a Cloudflare error page. Twilio TRIAL only texts Verified Caller IDs.

### Demo OTP + Become-a-partner (Jun 2026)
- OTP now runs in **DEMO MODE** (`OTP_DEMO_MODE=true`, code `123456`) — free, any number, no SMS. Flip to `false` for real Twilio SMS (paid account needed). `phone_auth.py` branches on DEMO_MODE.
- **Become a partner** replaces admin-generated invite links: public `/partner` page (`BecomePartner.jsx`) → `partner_applications` (pending). Admin "Partner applications" tab (`partners.py`, `/api/partners/apply`, `/api/admin/partners`, `/api/admin/partners/{id}/review`). Approve provisions the applicant's phone as an `analyst` (create/upgrade); they log in via phone-OTP → `/analyst`. Footer "Become a partner" → `/partner`. Admin "Analyst invites" tab removed from UI.
### Change brief v1 (22 Aug 2026) — 6 tickets, all verified 100% (testing iteration_2)
- **T1 (flicker/flash):** Home content hydrates from a `localStorage('bk_home_content_v1')` cache then revalidates from `/api/content`; admin `publish()` also writes that cache — so no hard-coded default flashes (fixes both the "Volatility" case flip and the stats value flash).
- **T2 (logo→home):** Navbar `Logo` and Footer brand link to `/` with smooth scroll-to-top (data-testid nav-logo-home / footer-logo-home).
- **T3 (left):** Nav wordings (Model Portfolios/AIF/Advisory/Learn) grouped left next to the logo; hero headline/sub/CTAs/ratings left-aligned (Home.css).
- **T4 (FAQ):** New `faq.py` (public `/api/faqs`, `/api/faqs/categories`, admin CRUD `/api/faqs*`), 6 seeded FAQs. Home FAQ accordion section (top FAQs) + `/faq` page (search + category filter + expand + contact CTA) + admin "FAQ" tab (create/edit/delete, category, order, isTop=Show on home, published).
- **T5 (footer):** `content.footer` block (contactEmail, subscribeHeading, socials{facebook,x,youtube,linkedin,instagram}) — admin-editable via "Site settings" tab + Publish. Footer shows "Find us on" 5 socials, contact email, and a Subscribe box (POST `/api/leads` type `subscribe`; leads.py now allows it). FAQ links added to footer.
- **T6 (home rail):** The 3-column Stock/ETF/MF showcase is driven by live `/api/portfolios` (bucketed by constituent type via `bucketOf`), each row links to its own `/model-portfolios/:id`; empty buckets show "No portfolios yet".
- Fix during test: added missing `Pencil` lucide import in AdminPage.jsx.

## Stack / Architecture
- Phone+OTP auth (Twilio Verify) is the public login for investors & analysts (backend/phone_auth.py). Admin stays email/password (internal). See CHANGELOG below.
- Frontend: React + TailwindCSS + React Router + shadcn/ui. `/app/frontend/src/pages/*`, `/app/frontend/src/components/*`.
- Backend: FastAPI (routers built per module) + Motor (MongoDB async). `/app/backend/server.py` includes routers.
- Auth: email/password JWT (PyJWT + bcrypt), RBAC roles: `admin`, `investor`, `analyst`. Users seeded idempotently on startup.
- Broker: Zerodha Kite Connect (`broker_kite.py`) — requires user API key/secret (KITE_API_SECRET missing in env → real order placement not fully live).

## Backend routers
- `auth.py`: POST /api/auth/signup, /api/auth/login, GET /api/auth/me
- `content.py`: GET/PUT /api/content (editable home content)
- `leads.py`: POST /api/leads/aif, /api/leads/advisory, GET /api/leads
- `analyst.py`: analyst profile + portfolios CRUD, submit; admin review (approve/reject); public /api/portfolios
- `broker_kite.py`: Zerodha Kite integration

## Key DB collections
- users {id, name, email, hashed_password, role, analyst_profile?, created_at}
- leads {id, type, email, plan?, created_at}
- content {type, data}
- analyst_portfolios {id, owner_id, owner_name, name, subtitle, strategy, risk, minAmount, subscription, constituents[], returns{}, factsheet{}, status(draft/pending/approved/rejected), review_note, created_at, updated_at}

## Credentials (see /app/memory/test_credentials.md)
- Admin: admin@omnivest.in / Admin@123
- Investor demo: demo@basketly.in / Password123

## Implemented (as of Jun 2026)
- Unified purple "Basketly" brand across all pages; exact basketly-home.html home layout (marquee phone mockup).
- Real JWT auth (signup/login/me) with seeded admin + demo users.
- AIF & Advisory lead-capture forms saving to backend; Leads visible in admin console.
- Admin console at /admin: edit home content (hero/stats/trust/testimonials) + Publish to live; Leads tab; Listings approval tab.
- Analyst console (role=analyst) at /admin: manage profile + portfolios, submit for review.
- Admin approval workflow VERIFIED end-to-end: analyst submit → admin approve → appears live on public /model-portfolios.
- ScrollToTop router fix; clickable model-portfolio feature rows.

### Added Jun 2026 (this session)
- **Factsheet PDF Upload**: analysts upload a PDF factsheet (Emergent Object Storage) per portfolio; investors download it from the public detail page. Backend: `storage.py`; endpoints POST/DELETE `/api/analyst/portfolios/{pid}/factsheet`, GET `/api/portfolios/{pid}/factsheet` (public if approved, else owner/admin). Frontend: AnalystConsole upload UI + ModelPortfolioDetail download card. VERIFIED (testing_agent iteration_1, 100%).
- **Analyst Invites**: admin-only invite links gate the `analyst` role. Self-signup as analyst now requires a valid, unused, unexpired invite (else becomes investor). Backend: `invites.py` (HMAC-digest storage, atomic find_one_and_update consume + compensating release), routes `/api/admin/invites` (create/list/revoke); `auth.py` signup accepts `invite_code`. Frontend: AdminPage "Analyst invites" tab + SignupPage invite banner. VERIFIED (100%, incl. reuse=403 gating).
- **UX polish**: admin/analyst login now routes straight to `/admin`; admin console header is tab-specific and hides Publish/Discard on management tabs (leads/listings/invites).
- **Baskets are analyst-only + demo samples hidden (Jun 2026)**: per user decision, admins can no longer create baskets (removed the admin "Baskets" creation tab; the tab now points admins to "Listings (approve)"). Basket creation stays with research analysts; admins review/approve. The public `/model-portfolios` page now shows ONLY real analyst-approved baskets (dropped the hardcoded demo `mock.js` baskets merge) with a proper loading + empty state. Home page decorative portfolio links repointed to `/model-portfolios`. Note: `mock.js` seed baskets still power secondary non-nav pages (Explore/Managers/Collections/Dashboard) which are not in the main navbar.
- **Admin Database viewer (Jun 2026)**: read-only, admin-only "Database" tab in `/admin`. Backend `dbadmin.py` (routes `/api/admin/db/collections`, `/api/admin/db/{collection}` with skip/limit/q, `/api/admin/db/{collection}/export` CSV, DELETE `/api/admin/db/{collection}/{id}`, POST `/api/admin/db/{collection}/clear`); allowlisted collections (users, leads, analyst_portfolios, analyst_invites, content, status_checks); recursively redacts sensitive fields (password/hash/secret/token). Admin user accounts are protected from deletion (single delete 403; clear users keeps role=admin). Frontend: collection pills w/ counts, search, expandable JSON records, pagination, **Export CSV**, per-record delete (two-step confirm), and **Clear collection** (AlertDialog confirm). Non-admins get 403. Works in production after redeploy (preview & prod DBs are separate).
- **About Us page (Jun 2026)**: new public `/about` route (`AboutPage.jsx`), linked from header nav + footer. Six sections — Hero, Our Story + stats strip, Meet the Team intro, Founders (expandable "Read Bio" + LinkedIn), Team + team stats, Investors + Get in touch — all admin-editable, no flash of stale content (skeleton until `/api/about` resolves). Backend `about.py`: singleton `site_content` doc key='about' with `GET /api/about` (defaults merge), admin `PUT /api/about` (top-level key whitelist), admin `POST /api/about/upload` (image → Emergent Object Storage, 8MB cap, image-types only), public `GET /api/about/media/{filename}` (served from `basketly/about/` prefix, traversal-guarded). Admin editor is a new **"About Us"** tab (`components/admin/AboutAdmin.jsx`) with its own Save (not the global Publish flow): section visibility toggles, hero, story+stats CRUD, team intro, founders/team CRUD w/ photo upload + reorder, team stats, investors (logos + people, hidden by default), and contact cards. Pre-filled with sample content; investors block hidden by default. Tested: backend 11/11 pytest (`/app/backend/tests/test_about.py`), frontend all flows green (iteration_3.json).

## Known / notes
- **Typography → Inter everywhere (Aug 2026)**: replaced Space Grotesk with Inter across the whole product per the Typography Brief (agreed with user — Space Grotesk's single-storey 'g'/straight 'y' read too playful for a trust-driven fintech; Inter matches Zerodha/Groww). Changes: `index.css` @import → Inter 400–800 (removed Space Grotesk); `index.html` link 600-only → 400–800; global `h1–h4` + `.num` font-family → Inter stack (kept `tabular-nums` on numbers); body set to 16px/400/line-height 1.7 with `'liga'/'calt'` + optical-sizing; all ~11 `font-[Space_Grotesk]` JSX usages (wordmark, dialog titles, About/Person headings) → `font-[Inter]`. In `Home.css`, the 8 large display headings (hero h1, band/hiw-head/hiw-copy/feat/trust/love/faq h2/h3) softened from weight 800→700 and tracking →‑0.01em (small bold labels/eyebrows/values kept at 800; font-sizes unchanged). Verified computed styles: hero = Inter/700/‑0.6px, body = Inter/16px/27.2px/400, About h1 + Admin wordmark = Inter; zero Space Grotesk references remain; no colour/layout/size regressions.
- **Scroll-dim depth effect (Aug 2026)**: added a rAF scroll handler in `Home.jsx` that computes how covered each stacked `.hiw-card` is (based on the gap to the next card's top) and sets a `--cover` (0→1) CSS var; `Home.css` uses it to `scale(1 - .05·cover)` + `brightness(1 - .16·cover)` + a dark `::after` overlay (opacity .22·cover) so covered cards recede with depth (premium fintech feel). Skipped under `prefers-reduced-motion`. Verified on desktop + mobile, pageScroll=0.
- **Sticky scroll-stack animation restored on mobile (Aug 2026)**: the Home "How it works" layered card stack (Model Portfolio → AIF → Advisory, `.hiw-*` in `Home.css`) is a `position:sticky` scroll-stack. It had been disabled on mobile (`position:static` @≤900px). Re-enabled it on mobile with compact sizing (peek 46px, phone 220×300, tighter padding) + a `prefers-reduced-motion` static fallback. **Root-cause fix**: the global mobile guard used `overflow-x:hidden` on html/body, which forces the cross-axis to `auto`, creates a scroll container, and **breaks `position:sticky` site-wide (including the desktop version of this animation)**. Changed the guard to `overflow-x:clip` in `index.css` — prevents horizontal page scroll AND preserves sticky. Verified: strips pin & stack with peeks on both desktop (1280) and mobile (390/320), pageScroll=0 across all 16 public routes at 320/375/390.
- **Mobile bottom tab bar (Aug 2026)**: `components/MobileBottomNav.jsx` rendered in `Layout.jsx` — sticky bottom bar, `lg:hidden`, 4 tabs (Home · Portfolios · Dashboard · Account) with active-state highlighting via `useLocation`, safe-area padding, and `pb-16 lg:pb-0` on the layout so content/footer clear the bar. Dashboard tab prompts login (openAuth) when unauthenticated; Account tab opens an upward Popover — authed: name/email + Connect broker + Log out; guest: Sign in / Get started. data-testids: mobile-bottom-nav, mobtab-home/portfolios/dashboard/account, mobnav-connect-broker/logout/signin. Verified: renders on mobile (4 tabs), hidden on desktop, pageScroll=0 across /, /model-portfolios, /about, /faq.
- **Mobile responsiveness pass (Aug 2026)**: implemented the full "Omnivest Mobile Audit Spec". Added a global CSS safety net in `index.css` (`html,body{overflow-x:hidden}` + `*{min-width:0}`), a dev-only overflow detector (`devOverflowCheck.js`, `window.__ck()`, gated to `NODE_ENV==='development'` — no-op in prod). Fixed root-cause patterns: Home hero font step-down (38px@600 / 32px@400) + sticky "how it works" panels made static ≤900px (`Home.css`); Footer subscribe row stacks (flex-col sm:flex-row); OrderTicket modal paired rows → `grid-cols-1 sm:grid-cols-2`; InvestFlow SIP row responsive + holdings table internal scroll; SIPCalculator stats `grid-cols-1 sm:grid-cols-3`; AnalystConsole editor grid `grid-cols-2 md:grid-cols-[...]`; ModelPortfolioDetail table `overflow-x-auto`; Explore filter aside sticky gated `lg:`. **AdminPage**: sidebar → horizontal scrollable top tab-bar on mobile (`grid-cols-1 lg:grid-cols-[240px_1fr]`, nav `flex lg:flex-col overflow-x-auto`) + content padding `p-4 sm:p-6 lg:p-8` + DB table `overflow-x-auto`. Added a **Trade button to the mobile navbar menu** (was desktop-only). Verified: ZERO page-level horizontal scroll across all 16 public routes + /dashboard + all 11 admin tabs at 390px and 320px (programmatic scrollWidth checks + testing_agent iteration_5.json, 100% of testable flows). OrderTicket/InvestFlow modals couldn't be opened E2E in preview (need broker-connected + a seeded approved portfolio) but their responsive classes are verified in source.
- **Omnivest rebrand (Aug 2026)**: renamed "Basketly"→"Omnivest" across frontend + backend (seeded copy in content.py/faq.py/about.py, auth.py display name "Omnivest Admin"), `mutlixx.in`/`support@mutlixx.in`→`omnivest.in`/`support@omnivest.in`, About contact emails→@omnivest.in. **Login emails**: admin=`admin@omnivest.in` (renamed from admin@basketly.in Jun 2026 — record migrated in place on startup, old email dead); demo investor=demo@basketly.in (kept, internal, not user-visible). Non-user-visible code identifiers kept unchanged: storage `APP_NAME="basketly"`, JWT default secret, localStorage keys (`basketly-*-v1`), postMessage source `basketly-kite-callback`. New Omnivest logo = white SVG mark (`src/assets/omnivest-mark-white.svg`, caring-hands O + pink heart) inside the existing purple grad-card box, in Navbar/Footer/AdminPage/AnalystConsole/AnalystPage/LoginPage/KiteCallback. Per user decision the **site's purple theme was kept unchanged** and the logo recoloured to match (no site-wide colour sweep). Metadata in `public/index.html`: title "Omnivest — All your investing, in one place", description, favicon set + OG/Twitter tags (og:image → https://omnivest.in/omnivest-og-1200x630.png). Purple favicons/app-icons/OG card generated with PIL into `public/` (favicon.svg/-16/-32/-48, apple-touch-icon, icon-192/512, omnivest-og-1200x630.png) + manifest.json. NOTE: OG image/favicon absolute URLs only resolve on the live omnivest.in domain after deploy + social-cache refresh.
- **Founder/Team dedicated pages (Aug 2026)**: About "Read Bio" no longer expands inline — it links to a dedicated route `/founders/:slug` or `/team/:slug` (`pages/PersonDetail.jsx`, two-column photo + name/role/full-bio + LinkedIn, "Back to About"). Slug = name lowercased, non-alnum→hyphens. Bogus slug → "Profile not found". Default founder is Tushar Sukhija (Founder & CEO) with the brief's bio. Tested green in iteration_4.json (backend 15/15, all frontend rebrand + routing + admin regression flows).
- The "Approve button not firing" bug from the prior fork was a FALSE ALARM (previous test selector matched sidebar
  "Listings (approve)" nav item, not the real Approve button). Approve/Reject work correctly end-to-end.
- Factsheet currently structured fields + pdfName metadata only; actual PDF file upload not implemented (backlog).
- KITE_API_SECRET missing → real broker order placement not fully live.

## Backlog
- **Nav & routing consolidation (Jun 2026, VERIFIED iteration_14):** (1) `Navbar.jsx` account dropdown is role-aware — admin → 'Admin console' (/admin), analyst → 'Analyst console' (/partner), investors → neither (desktop + mobile). (2) `/partner` is now the single partner URL: moved to a top-level route (out of Layout); logged-out visitors get the application form + a separated 'Already approved? Log in' CTA (opens PhoneAuthModal via openAuth({next:'/partner'})); approved analysts (role==='analyst') get `<AnalystConsole/>` rendered at /partner; `/analyst` → `<Navigate to='/partner'/>`; `AnalystPage` import removed from App.js (file left on disk, unused); BecomePartner success copy references omnivest.in/partner. (3) `AdminPage.jsx` sidebar reorganised into 5 collapsible groups (Partners & Listings / Site content / Investment catalog / Operations / System, System visually separated) with chevrons, localStorage persistence (`omni-admin-nav-groups-v1`), active item's group forced-open, and red pending-count badges on Partner applications (`pendingPartners`) and Listings-approve (`pendingListings`) via `refreshCounts()` on mount + after reviews. Route protection/permissions unchanged.
- **Become-a-partner v2 + phone country selector (Jun 2026, VERIFIED iterations 11–13):** Added `react-phone-number-input`; new shared `components/PhoneField.jsx` (flag + searchable country dropdown, default India +91, emits E.164, **hard-caps India at 10 digits via onKeyDown preventDefault** + limitMaxLength) used in BOTH `BecomePartner.jsx` and `PhoneAuthModal.jsx`. `phone_auth.py` now returns a clear message for unsupported-OTP-country Twilio codes (60078/60083/21408/21211). BecomePartner fully rewritten: required 'Applying as' (Individual/LLP/Company, no default), ALL fields mandatory incl. SEBI reg (`^IN[A-Z][0-9]{9}$`, 'optional' removed) and a required T&C consent checkbox whose link opens an admin-managed T&C modal; submit gated until valid. Backend `partners.py`: email required EmailStr, firm/note min_length 1, SEBI pattern, `applicant_type` field, `accepted_terms`+`accepted_terms_at` stored, server-side E.164 phone validation. `managers.py` carries `applicant_type` into the manager on approval (serializer exposes `applicantType`). Admin: `applicant_type` badge on the Partner-applications review card + Approved-Partners card; new editable 'Partner Terms & Conditions' block (`content.py` `partnerTerms` singleton in DEFAULT_CONTENT + ALLOWED_KEYS; editor in AdminPage Site-settings tab, part of the Publish flow) with a sensible placeholder default.
- **Admin Database tab rework (Jun 2026, VERIFIED iteration_10):** `dbadmin.py` now AUTO-DISCOVERS collections via `list_collection_names()` minus `_is_excluded` (system.* / EXCLUDED) — no more hard-coded whitelist (fixes the stale `content` entry; real collection is `site_content`). Redaction tuple expanded to `("password","hash","secret","token","code","otp")` (covers access_token/request_token/api secret/OTP/password hashes, esp. kite_sessions & broker_connections). Search `$or` adds `tradingsymbol/symbol/firm/question`. `clear_collection`: `broker_orders` → 403 (never clearable), and `users/partner_applications/managers/analyst_portfolios` require body `{confirm:<name>}` (400 otherwise). Frontend AdminPage: chips grouped into 5 labelled sections (People/Analyst/Broker & market/Site content/Settings & system, + Other fallback), `instruments` is search-first (`db-search-first`, no auto-list), Clear button hidden for broker_orders, and a typed-confirm Input (`db-clear-confirm-input`) gates clearing the 4 protected collections.
- **Admin email rename admin@basketly.in → admin@omnivest.in (Jun 2026):** `auth.py::seed_users` now seeds `admin@omnivest.in` and, on startup, renames the existing admin record IN PLACE (`update_one`, preserving id/password_hash/role, guarded by a destination-exists check + `DuplicateKeyError` catch) — no duplicate, no lockout. Verified: new email logs in, old email 401s, exactly 1 admin, bcrypt hash intact; 33/33 affected pytest pass. Also updated ADMIN_EMAIL in test_market_and_dropdowns/test_analyst_submit_gate/test_about/test_rebrand, and the Zerodha order tag default `"basketly"→"omnivest"` in broker_kite.py (user-visible in broker order book). demo@basketly.in and internal identifiers (JWT/invite pepper defaults, storage APP_NAME, localStorage keys, kite-callback postMessage source) intentionally left unchanged.
- **Feature 4 — Approved partners → Managers/"Approved Partners" (Jun 2026, VERIFIED iteration_9):** ROOT CAUSE of "approved Tushar doesn't show" — the admin console "Managers" tab rendered MOCK data (`seedManagers` from mock.js), never the DB. The backend approve→manager flow (`partners.py` → `managers.upsert_manager_from_partner`; reject → `deactivate_manager`) was already correct (curl-verified: approve adds w/ carried-over fields, re-approve no duplicate, reject sets active=false, public `/api/managers` shows active only). FIX: new `components/admin/ApprovedPartnersAdmin.jsx` fetches `/api/admin/managers` (Bearer token), lists each approved partner with editable firm/SEBI/philosophy/description/logo + Active toggle (`PUT /api/admin/managers/{id}`), empty state, testids `partner-row/save/active-<id>`. AdminPage: nav+header renamed **"Managers" → "Approved Partners"**, `managers` removed from CONTENT_TABS (own per-row Save, no global Publish), removed dead mock managers state. Public `/managers` page was already DB-backed (correct).
- **Feature 1/2/3 — Share previews + logo refinement (Jun 2026, VERIFIED iteration_8):**
  - **F3 logo:** refined the "O" from a chunky broken ring to a smooth slim taller ellipse with small side gaps + a clean pink heart (same purple gradient). Regenerated all raster assets from the SVG via `/app/scripts/gen_logo_assets.py` (cairosvg+PIL): favicon-16/32/48, apple-touch-icon, icon-192/512, and the 1200×630 OG image. Source SVGs: `frontend/src/assets/omnivest-mark-white.svg` (white, header/footer/console), `frontend/public/favicon.svg` (gradient).
  - **F2 share logo:** OG image regenerated to match the header (gradient card box + white slim-ellipse mark + "Omnivest" wordmark + tagline). Bumped OG/Twitter image URL to `?v=2` in `public/index.html`, `og.py`, `prerender-og.js` to force social-cache refetch of the new logo.
  - **F1 per-page share previews — ROOT CAUSE:** previous impl used only `react-helmet-async` (client-side JS); social crawlers don't run JS so every shared link showed the homepage preview. FIX: (1) NEW backend `og.py` → `GET /api/og?path=<path>` returns raw HTML with correct per-route OG/Twitter title+desc+image and a JS redirect to the real page for humans; handles dynamic `/model-portfolios/{id}` via DB lookup with graceful fallback. (2) `scripts/prerender-og.js` runs after `craco build` (build script updated in package.json) to write per-route `build/<route>/index.html` with baked meta so plain URLs also work on the Emergent static deploy. (3) `components/ShareButton.jsx` (Share button on ModelPortfolioDetail) copies/opens the `/api/og` link. Reusable pytest: `/app/backend/tests/test_og_share.py` (18/18).
  - **NOTE:** real crawler previews (WhatsApp/FB/LinkedIn/X) can only be confirmed AFTER an Emergent deploy + each platform's cache refresh — paste a full `https://omnivest.in/api/og?path=/about` (or the plain `https://omnivest.in/about`) into the platform's share-debugger.
- P1: F5.3 (admin re-listing change-delta) & F5.4 (rebalance trail timeline) — remaining Analyst Console build-out parts from the 29-Aug brief.
- P2: Full functional AIF/Advisory flows beyond lead capture.
- **Kite MARKET DATA — F6.1 + F6.4 (Aug 2026, VERIFIED iteration_7):** single shared "market-data" Kite session (admin connects once daily; token expires ~6 AM IST). Backend `market_data.py`: admin `GET/POST /api/admin/kite/market/{login-url,exchange,status,refresh-instruments,disconnect}` (session in `kite_sessions` account="market-data"); analyst `GET /api/market/instruments/search` (cached `instruments` collection, NSE/BSE prefix search), `POST /api/market/quote` (LTP + prev close + change%), `POST /api/market/period-return` (1Y/3Y/5Y CAGR + return from historical_data). `TokenException` → 503 + `needs_reconnect` flag. Frontend: `MarketDataAdmin.jsx` (admin "Market data (Kite)" tab — connect/reconnect, refresh instruments, status); `AnalystConsole.jsx` `InstrumentPicker` typeahead per constituent (stores exchange), "Fetch live prices" + "Auto-fill from live data" (weight-weighted returns). `KiteCallback.jsx` branches on `sessionStorage.kite_flow==='market'` → admin exchange. Keys in backend/.env (KITE_API_KEY/SECRET). **LIVE data untested — requires admin's real daily Zerodha browser login; the not-connected 503 path is verified.**
- **Editable Dropdowns — F5.1 (Aug 2026, VERIFIED iteration_7):** admin "Form dropdowns" tab (`DropdownsAdmin.jsx`) edits the analyst listing-form option lists (strategy, risk, rebalanceFreq, subscription, constituentType). Backend `listing_options.py`: `GET /api/listing-options` (public, defaults fallback), `PUT /api/admin/listing-options` (admin, non-empty + case-insensitive de-dupe); `app_settings` doc `_id=listing_options`. AnalystConsole renders all dropdowns from these.
- **Draft Reminders (Aug 2026, VERIFIED iteration_7):** analyst portfolio list shows a per-draft/rejected checklist (`data-testid=draft-checklist`) — "N items left before you can submit" (expandable, mirrors `validateForSubmit`) or "✓ Ready to submit".
- **29-Aug brief Phase 3 — Analyst Console validations (Aug 2026, VERIFIED iteration_6):** F5.2 subtitle hard-capped at 30 words; F6.2 total weight must equal exactly 100%; F6.3 all fields mandatory on submit; strict validation only on "Save & submit"; backend gate in `analyst.py::_validate_complete()`. Reusable pytest: `/app/backend/tests/test_analyst_submit_gate.py`, `/app/backend/tests/test_market_and_dropdowns.py`.
